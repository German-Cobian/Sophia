<%= form_for([@article, @article.comments.build]) do |f| %>
  <div class=''>
    <div class="d-flex flex-row">
      <%= f.text_area :body, placeholder: 'Comment', size: "100x3",class:'mt-3' %>
    </div>
    <%= f.submit class:'btn btn-outline-primary py-0 mt-2'%>
  </div>
<% end %>
